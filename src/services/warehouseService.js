/**
 * Warehouse Layout & Zones Configuration API Service
 * 
 * TODO: Integrate with backend warehouse layout endpoints.
 */

export const addZoneApi = async (zone) => {
  // TODO: Call POST /warehouse/zones
  return { success: true };
};

export const addBinApi = async (bin) => {
  // TODO: Call POST /warehouse/bins
  return { success: true };
};
